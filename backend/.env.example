# Database: use SQLite for local dev, PostgreSQL for production
# SQLite (local):
DATABASE_URL=sqlite:///./hrms_lite.db
# PostgreSQL (production example):
# DATABASE_URL=postgresql://user:password@host:5432/hrms_lite

# Allowed frontend origins (JSON array). Add your Vercel URL when deploying backend.
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# API prefix (optional; default is /api/v1)
API_V1_PREFIX=/api/v1
